apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: churn-model

resources:
  - serviceaccount.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: churn-model-api
  app.kubernetes.io/component: api
  app.kubernetes.io/part-of: churn-prediction-system

images:
  - name: churn-model-api
    newName: <AWS_ACCOUNT_ID>.dkr.ecr.<AWS_REGION>.amazonaws.com/churn-model-api
    newTag: latest
